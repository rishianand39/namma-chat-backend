model Otp {
  id         String   @id @default(uuid())
  phone      Int
  code        Int
  expires_at  DateTime
  verified   Boolean  @default(false)
  created_at  DateTime @default(now())

  @@index([phone])
}